minio:
  url: ${MINIO_URL}
  access-key: ${MINIO_ACCESS_KEY}
  secret-key: ${MINIO_SECRET_KEY}
  bucket: ${MINIO_BUCKET}

user-key:
  service-name: user-service
  url: lb://user-service
  get-public-key: /api/users/get/public-key/

