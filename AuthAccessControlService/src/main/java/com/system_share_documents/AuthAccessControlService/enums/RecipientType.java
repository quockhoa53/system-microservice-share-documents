package com.system_share_documents.AuthAccessControlService.enums;

public enum RecipientType {
    USER,    // Recipient là một user cụ thể
    GROUP    // Recipient là một group (sử dụng recipientGroupId)
}
