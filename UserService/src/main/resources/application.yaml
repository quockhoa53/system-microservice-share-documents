server:
  port: 8081
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: false
spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://ep-solitary-forest-adhhauxm-pooler.c-2.us-east-1.aws.neon.tech:5432/user_db?user=neondb_owner&password=npg_xdHw4FNo1XRC&sslmode=require
    username: neondb_owner
    password: npg_xdHw4FNo1XRC
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9090/realms/system-share-docs
app:
  security:
    keycloak:
      resource-client-id: frontend-app