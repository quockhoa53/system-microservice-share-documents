<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="user-service"/>
    <springProperty scope="context" name="graylogHost" source="graylog.host" defaultValue="localhost"/>
    <springProperty scope="context" name="graylogPort" source="graylog.port" defaultValue="12201"/>
    <springProperty scope="context" name="graylogEnabled" source="graylog.enabled" defaultValue="true"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <if condition='property("graylogEnabled").equals("true")'>
        <then>
            <appender name="GELF" class="de.siegmar.logbackgelf.GelfUdpAppender">
                <graylogHost>${graylogHost}</graylogHost>
                <graylogPort>${graylogPort}</graylogPort>
                <maxChunkSize>508</maxChunkSize>
                <encoder class="de.siegmar.logbackgelf.GelfEncoder">
                    <includeRawMessage>false</includeRawMessage>
                    <includeMarker>true</includeMarker>
                    <includeCallerData>false</includeCallerData>
                    <includeRootCauseData>true</includeRootCauseData>
                    <includeLevelName>true</includeLevelName>
                    <shortPatternLayout class="ch.qos.logback.classic.PatternLayout">
                        <pattern>%m%n</pattern>
                    </shortPatternLayout>
                    <fullPatternLayout class="ch.qos.logback.classic.PatternLayout">
                        <pattern>%m%n</pattern>
                    </fullPatternLayout>
                    <staticField>application:${appName}</staticField>
                    <staticField>environment:${spring.profiles.active:default}</staticField>
                    <staticField>stream:watermark-worker-logs-api</staticField>
                </encoder>

                <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                    <level>INFO</level>
                </filter>

                <filter class="ch.qos.logback.core.filter.EvaluatorFilter">
                    <evaluator>
                        <expression>
                            message.contains("HikariPool") ||
                            message.contains("Tomcat") ||
                            message.contains("o.s.b.a.e") ||
                            message.contains("o.s.c.n.eureka") ||
                            message.contains("org.springframework.cloud") ||
                            message.contains("org.springframework.web") ||
                            message.contains("[Consumer clientId") ||
                            message.contains("[Producer clientId") ||
                            message.contains("Node -1 disconnected") ||
                            message.contains("Node -1 connected") ||
                            (level == DEBUG &amp;&amp; !message.contains("[requestId="))
                        </expression>
                    </evaluator>
                    <onMatch>DENY</onMatch>
                    <onMismatch>NEUTRAL</onMismatch>
                </filter>
            </appender>
        </then>
    </if>

    <logger name="com.system_share_documents" level="INFO"/>

    <logger name="org.springframework" level="WARN"/>
    <logger name="org.springframework.web" level="WARN"/>
    <logger name="org.springframework.cloud" level="WARN"/>
    <logger name="org.springframework.security" level="WARN"/>
    <logger name="org.springframework.kafka" level="WARN"/>

    <logger name="org.hibernate" level="ERROR"/>
    <logger name="org.hibernate.SQL" level="WARN"/>

    <logger name="com.netflix" level="WARN"/>

    <logger name="org.apache.kafka" level="WARN"/>

    <logger name="de.siegmar.logbackgelf" level="WARN"/>

    <logger name="com.system_share_documents.AppCommonService.filter.HttpLoggingFilter" level="INFO" additivity="false">
        <appender-ref ref="GELF"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="GELF"/>
    </root>

</configuration>
